<?xml version="1.0" encoding="utf-8"?>
<!-- Lista de usuarios con los que iniciar una conversación privada -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/fondo_oscuro">

    <!-- Cabecera -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingTop="20dp"
        android:paddingBottom="16dp"
        android:paddingHorizontal="16dp"
        android:background="@color/fondo_superficie">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnAtras"
            style="@style/Widget.MaterialComponents.Button.OutlinedButton"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:insetTop="0dp"
            android:insetBottom="0dp"
            android:padding="0dp"
            android:text=""
            app:icon="@drawable/ic_arrow_back"
            app:iconSize="22dp"
            app:iconGravity="textStart"
            app:iconPadding="0dp"
            app:iconTint="@color/verde_lima"
            app:strokeColor="@color/verde_lima"
            app:strokeWidth="2dp"
            app:cornerRadius="22dp"
            android:backgroundTint="@android:color/transparent"
            android:contentDescription="@string/btn_atras" />

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="💬  Chat con Amigos"
            android:textColor="@color/verde_lima_brillante"
            android:textSize="20sp"
            android:textStyle="bold"
            android:layout_marginStart="8dp" />

    </LinearLayout>

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/fondo_tarjeta" />

    <!-- Texto informativo sobre el funcionamiento del chat -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:text="Selecciona un jugador para enviarle un mensaje privado."
        android:textColor="@color/texto_gris"
        android:textSize="13sp" />

    <!-- Lista de usuarios (se rellena dinámicamente desde ChatListActivity) -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <LinearLayout
            android:id="@+id/llUsuarios"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical" />

    </ScrollView>

</LinearLayout>
